const INCORRECT_DATA = 'Переданы некорректные данные';

const INCORRECT_EMAIL_PASSWORD = 'Неправильные почта или пароль';

const INCORRECT_DATA_CREATE = 'Переданы некорректные данные при создании пользователя';

const INCORRECT_DATA_PATCH = 'Переданы некорректные данные при обновлении профиля';

const DELETE_SOMEONE_MOVIE = 'Попытка удалить чужой фильм';

const DELETE_MOVIE = 'Фильм удалён';

const EMAIL_ALREADY_USE = 'Данный email уже используется';

const USER_NOT_FOUND = 'Пользователь по указанному id не найден';

const NONEXISTENT_MOVIE_ID = 'Передан несуществующий id фильма';

const PAGE_NON_FOUND = 'Страница не найдена';

const AUTHORIZATION_REQUIRED = 'Необходима авторизация';

module.exports = {
  INCORRECT_EMAIL_PASSWORD,
  EMAIL_ALREADY_USE,
  INCORRECT_DATA_CREATE,
  INCORRECT_DATA_PATCH,
  USER_NOT_FOUND,
  INCORRECT_DATA,
  NONEXISTENT_MOVIE_ID,
  DELETE_SOMEONE_MOVIE,
  DELETE_MOVIE,
  PAGE_NON_FOUND,
  AUTHORIZATION_REQUIRED,
};
